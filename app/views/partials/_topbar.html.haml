.topbar-wrapper{:style => "z-index:5;"}
  .topbar
    .topbar-inner
      .fill
        .container
          .brand
            Quirkweek
          %ul.nav.secondary-nav
            -unless current_user && user_has_authentication('twitter')
              %li
                =link_to "Sign in with Twitter", "/auth/twitter"
            -unless current_user && user_has_authentication('facebook')
              %li
                =link_to "Sign in with Facebook", "/auth/facebook"
            -unless current_user
              %li
                =link_to "Signup", signup_path
              %li
                =link_to "Login", login_path
                
          -if current_user
            %ul.nav.secondary-nav
              %li
                =link_to "Sign Out", signout_path
            %p.pull-right
              Logged in as
              =link_to "#{current_user.name}", "#"

          / %ul.nav.secondary-nav
          /   -unless current_user
          /     %li
          /       =link_to "Sign in with Twitter", "/auth/twitter"
          /     %li
          /       =link_to "Sign in with Facebook", "/auth/facebook"
          /     %li
          /       =link_to "Signup", signup_path
          /     %li
          /       =link_to "Login", login_path
          / 
          / -if current_user
          /   %ul.nav.secondary-nav
          /     %li
          /       =link_to "Sign Out", signout_path
          /   %p.pull-right
          /     Logged in as
          /     =link_to "#{current_user.name}", "#"
